# Knowledge

Durable, reusable subject knowledge belongs here.

Domains:
- `ai/`
- `data/`
- `development/`
- `banking/`
- `career/`

Use `templates/knowledge-entry.md` for new canonical notes. Keep project-specific state under `projects/`, stable cross-project context under `memory/`, and reusable instructions under Skills or `prompts/`.
