# Project Memory

Source of truth: `/.agents/skills/project-memory/SKILL.md`

Purpose: make project continuation deterministic by keeping current status, decisions, bugs, roadmap, and references in repository files instead of chat history.

Compatibility: designed around the open Agent Skills `SKILL.md` pattern. Agents without automatic `.agents/skills/` discovery can read the source file directly.
